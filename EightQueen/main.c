#include <stdio.h>
#include <stdlib.h>

int v, i, j, k, l, s, a[99];
int main(void)
{
    for (scanf("%d", &s); *a - s;
            v = a[j *= v] - a[i],
            k = i < s,
            j += (v=j<s && (!k && printf("\n\n%c" + 2 - (!l << !j),
                                           "*#Q"[l^v? (1^j) & 1 : 2])
                            && ++l || a[i] < s && v && v - i + j))
                && !(l %= s), v||( i==j? a[i+=k] = 0: ++a[i]) >= s * k && ++a[--i])
        continue;

    return 0;
}
